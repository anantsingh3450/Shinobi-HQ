# Scripts

Development and operational scripts (future).

Planned:

- `setup.sh` / `setup.ps1` — environment bootstrap
- `run_hokage.sh` — start the commander
- `seed_data.py` — load sample data for development

No scripts yet.
